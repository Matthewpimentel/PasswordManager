<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded" Closing="Window_Closing"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Label Content="Password Manager" Grid.Column="1" Grid.Row="0"  HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="30"/>
        <StackPanel Grid.Column="2" Orientation="Horizontal">
            <Label Content="Search: " VerticalAlignment="Center"/>
            <TextBox x:Name="searchBox" Height="15" Width="100" Grid.Column="2" VerticalAlignment="Center" TextChanged="TextBox_TextChanged"/>
        </StackPanel >
        <ListView x:Name="passwords" Grid.Column="1" Grid.Row="1">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Program" Width="125" DisplayMemberBinding="{Binding Program}"/>
                    <GridViewColumn Header="Username" Width="125" DisplayMemberBinding="{Binding Username}"/>
                    <GridViewColumn Header="Password" Width="124" DisplayMemberBinding="{Binding Pw}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Column="2" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Content="Program/Website: "  Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
            <TextBox x:Name="programName" Grid.Row="1" Width="120" Height="20" VerticalAlignment="top"/>
            <Label Content="Username: " Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
            <TextBox x:Name="userName" Grid.Row="3" VerticalAlignment="Top" Width="120" Height="20"/>
            <Label Content="Password: " HorizontalAlignment="Center" Grid.Row="4" VerticalAlignment="Bottom"/>
            <TextBox x:Name="passwordInput" Grid.Row="5" Width="120" Height="20" VerticalAlignment="top"/>
            <Button Content="Add Password" Grid.Row="6" Width="110" Height="25" Click="Button_Click"/>
        </Grid>
        <Button Content="Delete" Grid.Column="0" Grid.Row="1" Width="100" Height="30" Click="DeletePassword"/>
    </Grid>
</Window>
